/** Native Pi compaction is no longer an AgentSession operation. */
export const NATIVE_COMPACTION_DISABLED_CODE = "native_compaction_disabled";

export const NATIVE_COMPACTION_DISABLED_MESSAGE =
	"Native Pi compaction operations are disabled; Context Strategy owns real_context.write";
